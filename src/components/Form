import React, { useState } from 'react';

const Form = () => {
    const [value, setValue] = useState('');
    
    return(
        <form action="">
            <input type="textarea" placeholder="Add event description"/>
        </form>
    )
}

export default Form;